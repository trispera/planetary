# The namespace for tasks processed by planetary
{{- if not (.Capabilities.APIVersions.Has "security.openshift.io/v1") }}
apiVersion: v1
kind: Namespace
metadata:
  name: {{ .Values.tasks.namespace }}
{{- end }}
